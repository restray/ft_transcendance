import { UserfieldsInterceptor } from './userfields.interceptor';

describe('UserfieldsInterceptor', () => {
  it('should be defined', () => {
    expect(new UserfieldsInterceptor()).toBeDefined();
  });
});
